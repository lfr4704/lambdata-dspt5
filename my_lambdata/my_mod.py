def enlarge(n):

    return n* 100

if __name__ = "__main__":
    #only row the code below if executing this script from the cmd line
    #otherwise do not run it (for example if we are trying to import something)
    x = 5
    print(enlarge(x))

    y = int(input("Please choose a number (e.g. 5):"))
    print(enlarge(y))
